/** Render the persisted terms used by customer signing, never current account estimates. */
export function settlementPreviewHtml(s: {id:string;total:unknown;agreementText:string;authorizationText:string;scheduleJson:string}) {
  const escape = (value:unknown) => String(value).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));
  const schedule = JSON.parse(s.scheduleJson) as {date:string;amount:number}[];
  const original = `/api/settlement-contract/${encodeURIComponent(s.id)}`;
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>PennyLime settlement contract</title><style>body{font:15px/1.6 system-ui,sans-serif;color:#18181b;margin:0;background:#f6f8f6}main{max-width:850px;margin:24px auto;background:white;padding:28px}h1,h2{color:#15803d}pre{white-space:pre-wrap;font:inherit}table{width:100%;border-collapse:collapse}td,th{padding:10px;border-bottom:1px solid #ddd;text-align:left}td:last-child,th:last-child{text-align:right}iframe{width:100%;height:650px;border:1px solid #ddd}a{color:#15803d}@media print{body{background:white}main{margin:0;padding:0}iframe{display:none}}</style></head><body><main><h1>PennyLime settlement contract</h1><h2>$${escape(Number(s.total).toFixed(2))} · ${schedule.length} payments</h2><p>This is the settlement offered to the client. The revised terms and schedule below amend the original agreement included afterward.</p><pre>${escape(s.agreementText)}</pre><h2>Replacement payment schedule</h2><table><thead><tr><th>Payment date</th><th>Amount</th></tr></thead><tbody>${schedule.map(p=>`<tr><td>${escape(p.date)}</td><td>$${escape(Number(p.amount).toFixed(2))}</td></tr>`).join('')}</tbody><tfoot><tr><th>Total</th><th>$${escape(Number(s.total).toFixed(2))}</th></tr></tfoot></table><h2>ACH authorization</h2><pre>${escape(s.authorizationText)}</pre><h2>Original agreement — amended by the settlement above</h2><p>The original payment schedule is replaced by the schedule above once the client signs the settlement.</p><a href="${original}" target="_blank" rel="noopener noreferrer">Open original signed agreement (PDF)</a><iframe title="Original agreement supporting the settlement" src="${original}"></iframe></main></body></html>`;
}
